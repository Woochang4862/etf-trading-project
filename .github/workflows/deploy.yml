name: Deploy to ahnbi2 Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy to ahnbi2 server
        run: |
          ssh ahnbi2@ahnbi2.suwon.ac.kr "cd ~/etf-trading-project && git pull origin main && ./start.sh"
          echo "Deployment completed at $(date)"
